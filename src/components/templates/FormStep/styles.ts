import styled from "styled-components/native";
import { scale, verticalScale } from "react-native-size-matters";

export const StyledFormStepContainer = styled.View`
  margin-top: ${verticalScale(76)}px;
  flex: 1;
  align-items: center;
`;
